# 🚀 Быстрый старт

## Проблема с портом 8081?

Если видите сообщение "Port 8081 is running", выполните:

```bash
# Остановить все процессы Expo
pkill -f "expo start"

# Или освободить порт вручную
lsof -ti:8081 | xargs kill -9

# Затем запустите снова
cd /Users/oneofthepixels/Projects/price-compare-il
npx expo start --clear
```

## Альтернатива: использовать другой порт

Если порт занят, просто нажмите `Y` когда Expo спросит "Use port 8082 instead?"

## Запуск проекта

```bash
cd /Users/oneofthepixels/Projects/price-compare-il
npm install    # если еще не установлено
npm start      # или npx expo start
```

## Полезные команды

```bash
npm run ios      # Запуск на iOS симуляторе
npm run android  # Запуск на Android эмуляторе
npm run web      # Запуск веб-версии
```

## Отладка

Если проект не запускается:
1. Убедитесь, что установлены зависимости: `npm install`
2. Очистите кэш: `npx expo start --clear`
3. Проверьте порты: `lsof -i:8081`
